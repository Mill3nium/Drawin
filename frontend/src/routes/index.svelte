<script lang="ts">
//   import gallery from './gallery';

//   const load = (src: string) => () => {
//     if (!canvas) return;

//     const ctx = canvas.getContext('2d');

//     if (!ctx) return;

//     const image = document.createElement(
//       'img'
//     );
//     image.setAttribute('src', src);

//     ctx.drawImage(image, 0, 0);
//   };
</script>

<svelte:head>
	<title>Drawin - Start</title>
</svelte:head>

<main>
    <!-- <h2>Previous whiteboards:</h2>
    <div class="gallery">
      {#each $gallery as art}
        <button
          class="saved"
          on:click={load(art)}
        >
          <img
            src={art}
            alt="Oops, couldn't load!"
          />
        </button>
      {/each}
    </div> -->
</main>